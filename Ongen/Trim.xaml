<ContentDialog
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ongen"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
             x:Class="Ongen.Trim"
    Title="Trim"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <!-- Content body -->
        <TextBlock Text="Under construction!" />
        <Slider HorizontalAlignment="Stretch" x:Name="numericLeft" Value="{x:Bind startPos}" />
        <Slider HorizontalAlignment="Stretch" x:Name="numericRight" Value="{x:Bind endPos}" />
    </StackPanel>
</ContentDialog>
