<ContentDialog     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ongen"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
             x:Class="Ongen.SelectKey"
    Title="Bind a new play key"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    >

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <!-- Content body -->
        <ComboBox HorizontalAlignment="Stretch" SelectedItem="{x:Bind selectedKey}" PlaceholderText="Select the key you want to use" ItemsSource="{x:Bind keys}" />
    </StackPanel>

</ContentDialog>
